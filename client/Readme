UI/US: launch qr reader at app start.. if bool(checked-in)==true, show check-out button


    [] generate eth account
    [] unlock eth account
    [] call contract functions
        [] send() call to signCheckIn(organizationId, locationId)
        [] call() sign checkOut()
