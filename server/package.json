{
  "name": "poa-server",
  "version": "1.0.0",
  "description": "A decentralized protocol to track employees/people's attendance built on the Ethereum blockchain (and ipfs).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nfl0",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "web3": "^1.3.4"
  }
}
